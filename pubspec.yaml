name: business_pro
description: "A professional Business Management System."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # خليناها كدة عشان تقبل أي تحديث جديد

dependencies:
  flutter:
    sdk: flutter

  # --- 1. الأساسيات (Core) ---
  provider: ^6.1.1           # لإدارة الحالة (State Management)
  shared_preferences: ^2.2.2 # لحفظ الإعدادات البسيطة
  intl: ^0.19.0              # لضبط التواريخ والعملات
  google_fonts: ^6.1.0       # الخطوط (Cairo)

  # --- 2. قاعدة البيانات (Database) ---
  sqflite: ^2.3.0
  path: ^1.8.3

  # --- 3. الطباعة والملفات (Printing & PDF) ---
  pdf: ^3.10.4
  printing: ^5.11.0
  path_provider: ^2.1.1      # مهمة جداً لحفظ ملفات PDF
  open_file: ^3.3.2          # لفتح الملفات بعد الحفظ
  file_picker: ^8.0.0  # بدلاً من 6.2.1  excel: ^4.0.0
  flutter_local_notifications: ^17.2.2  # أو أحدث نسخة متاحة
  excel: ^4.0.0  # 👈 مهم جداً يكون الإصدار 4.0.0 أو أحدث
  supabase_flutter: ^2.3.0  # مكتبة سوبابيز
  url_strategy: ^0.2.0      # عشان يشيل الـ # من اللينك في الويب

  # --- 4. واجهة المستخدم (UI & UX) ---
  cupertino_icons: ^1.0.6
  flutter_slidable: ^3.0.1   # عشان السحب للحذف (Swipe to delete)
  badges: ^3.1.2             # للأرقام فوق الأيقونات (Notifications)
  animated_text_kit: ^4.2.2  # حركات للنصوص
  percent_indicator: ^4.2.3  # دوائر التحميل والنسب
  flutter_staggered_animations: ^1.1.1 # أنيميشن لظهور القوائم

  # --- 5. الرسوم البيانية (Charts) ---
  # تم تحديث الإصدارات لتتوافق مع فلاتر 3
  syncfusion_flutter_charts: ^24.1.41
  syncfusion_flutter_core: ^24.1.41
  syncfusion_flutter_datagrid: ^24.1.41
  
  # --- 6. الإشعارات والتقويم (Extra) ---
  timezone: ^0.9.2
  table_calendar: ^3.0.9
  flutter_colorpicker: ^1.0.3
  sqflite_common_ffi: ^2.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # مكتبة لتغيير أيقونة التطبيق بسهولة (اختياري)
  flutter_launcher_icons: ^0.13.1

flutter:
  assets:
    - assets/logo.png

  uses-material-design: true
  
  # تأكد إنك ضايف اللوجو بتاعك هنا لو موجود
  # assets:
  #   - assets/logo.png

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo.png" # 👈 ده مسار صورتك
  min_sdk_android: 21
  windows:
    generate: true
    icon_size: 48